"""Helper scripts shipped alongside the runtime package.

These modules are importable from the CLI (``ralf-free install-agent``
delegates to ``install_hook.install_for_agent``) and also runnable as
scripts via ``python3 -m ralf.scripts.compile_rules``.
"""
